DEBUG=True
SECRET_KEY=change-me-in-production
DB_NAME=pp
DB_USER=pp
DB_PASSWORD=pp-2026
DB_HOST=localhost
DB_PORT=5432
# Distributed lock / cache (Req 7 & 6). If unreachable, the lock falls back to
# an in-process lock so demos still run. Start one with:
#   docker run --rm -p 6379:6379 redis
REDIS_URL=redis://localhost:6379/0
