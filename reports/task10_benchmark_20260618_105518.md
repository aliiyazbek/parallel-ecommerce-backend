# Task 10 — Benchmarking & Bottleneck Analysis

**Generated:** 2026-06-18T10:55:18  
**Catalogue size:** 214 active products  
**Rows per listing:** 200  
**Repetitions:** 30

## 1. Key operation — measured response time

Live endpoint latency for the catalogue listing (the most-hit read path):

| Operation | Status | p50 (ms) | p95 (ms) | mean (ms) |
|-----------|:------:|---------:|---------:|----------:|
| `GET /api/products/` | 200 | 4.7 | 6.8 | 5.0 |

## 2. Bottleneck identified — N+1 queries

`ProductSerializer` exposes `category_name` through `source="category.name"`. Listing N products **without** `select_related("category")` issues **one query for the products plus one extra query per product** to fetch its category — the classic **N+1 query problem**. It scales linearly with catalogue size and is the dominant cost of the listing endpoint.

## 3. Before vs After the fix

Both variants serialize the same **200 products**, averaged over **30** runs.

| Variant | DB queries | Mean (ms) | p50 (ms) |
|---------|-----------:|----------:|---------:|
| **BEFORE** — no `select_related` | 201 | 97.77 | 97.03 |
| **AFTER** — `select_related("category")` | 1 | 16.68 | 15.14 |

### Improvement

| Metric | Before | After | Gain |
|--------|-------:|------:|:-----|
| DB queries | 201 | 1 | **−200 (100% fewer)** |
| Mean latency | 97.77 ms | 16.68 ms | **5.86× faster (−83%)** |

## 4. The fix

```python
# products/views.py — ProductViewSet
queryset = Product.objects.filter(is_active=True).select_related("category")
```

- Bottleneck + benchmark harness: `orders/management/commands/benchmark.py`
- Applied fix: `products/views.py::ProductViewSet.queryset`
- Always-on timing: the AOP `@log_execution` decorator (`core/aop.py`) logs per-call latency in production.
