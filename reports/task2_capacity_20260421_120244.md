# Task 2 — Resource Management & Capacity Control

**Generated:** 2026-04-21T12:02:44

## Defence layers
- **Layer 1 — DRF ScopedRateThrottle** (5/min per user on `/api/coupons/redeem/`). Protects against a single user flooding an endpoint. Over-quota requests return **HTTP 429** before touching business logic.
- **Layer 2 — `threading.BoundedSemaphore`** (bulkhead). Caps the total number of simultaneous heavy operations. Excess callers wait up to a short timeout and fail fast with **HTTP 503** instead of oversubscribing a finite resource.

## Layer 2 — Bounded concurrency cap (Before vs After the fix)

Each scenario launches **30** threads at a finite resource pool of **10** slots. **BEFORE FIX** hits the pool directly; **AFTER FIX** goes through `@limit_concurrency(max_concurrent=3, timeout=2.0s)`.

| Scenario | Threads | Wall (ms) | OK | Pool-exhausted | HTTP 503 | p50 total | p95 total | p50 wait | p95 wait |
|----------|--------:|----------:|---:|---------------:|---------:|----------:|----------:|---------:|---------:|
| BEFORE FIX  —  no capacity control   (30 ops, pool=10) | 30 | 211 | 10 | 20 | 0 | 201 | 201 | 0 | 0 |
| AFTER FIX   —  @limit_concurrency(cap=3)  (30 ops, pool=10, timeout=2.0s) | 30 | 2024 | 30 | 0 | 0 | 1102 | 2005 | 902 | 1805 |

### Side-by-side comparison

| Metric | BEFORE FIX | AFTER FIX |
|--------|-----------:|----------:|
| Threads | 30 | 30 |
| Succeeded | **10/30 (33%)** | **30/30 (100%)** |
| Pool-exhausted (crashed) | 20 | 0 |
| Rejected (HTTP 503) | 0 | 0 |
| Wall time (ms) | 211 | 2024 |
| p50 latency (ms) | 201 | 1102 |
| p95 latency (ms) | 201 | 2005 |

**Verdict:** success rate jumped from **33%** to **100%** with zero change to the underlying resource — only admission control was added.

## Interpretation
- **Without cap**: every thread races to the pool at once. The pool fills, the overflow fails with *pool-exhausted*. Fast but **unreliable under load**.
- **With cap**: the semaphore admits only N callers at a time. Excess traffic queues briefly; nothing oversubscribes the pool. Slightly higher tail latency, **100% success**.

## Where it lives in the codebase
- Primitive: `core/concurrency.py::limit_concurrency`
- Applied: `orders/views.py::CheckoutView.post` (`@limit_concurrency(max_concurrent=3, timeout=2.0)`)
- Rate limit: `config/settings.py::REST_FRAMEWORK``["DEFAULT_THROTTLE_RATES"]`
